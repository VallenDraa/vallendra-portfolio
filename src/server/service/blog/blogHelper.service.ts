// import BlogPostModel from "server/mongo/model/blogPost.model";

// // export default async function checkIfBlogExistAndUpdate(slug: string) {
// //   const blogExist = await BlogPostModel.exists({ slug });
// // }
